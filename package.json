{
  "name": "tigertix",
  "version": "1.0.0",
  "description": "Clemson TigerTix ticket management system",
  "main": "TigerTix.js",
  "type": "module",
  "scripts": {
    "start": "cd frontend && npm start",
    "build": "cd frontend && npm run build",
    "vercel-build": "cd frontend && npm run build",
    "start:all": "concurrently -k -n admin,client,llm,auth,frontend \"cd backend/admin-service && node server.js\" \"cd backend/client-service && node server.js\" \"cd backend/llm-booking-service && node server.js\" \"cd backend/user-authentication && npm start\" \"cd frontend && npm start\"",
    "test": "cross-env NODE_OPTIONS=--experimental-vm-modules jest --runInBand",
    "postinstall": "npm ci --prefix backend/user-authentication && npm ci --prefix backend/client-service && npm ci --prefix backend/admin-service && npm ci --prefix backend/llm-booking-service && npm ci --prefix frontend || true"
  },
  
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cookie-parser": "^1.4.7",
    "cors": "^2.8.5",
    "express": "^4.21.2",
    "jsonwebtoken": "^9.0.2",
    "sqlite3": "^5.1.7"
  },
  "devDependencies": {
    "concurrently": "^8.0.0",
    "cross-env": "^7.0.3",
    "jest": "^29.7.0",
    "supertest": "^7.1.4"
  }
}
